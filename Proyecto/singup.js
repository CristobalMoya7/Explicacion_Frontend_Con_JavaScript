
const singupForm = document.querySelector('#register-form')

singupForm.addEventListener('submit', (event) => {
    event.preventDefault(); // El preventDefault es para evitar la validacion en el servidor

    // Validaciones del formulario

    // Email con formato correcto

    // Las dos password deben coincidir
})